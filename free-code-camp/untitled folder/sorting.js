
function sumAll(arr) {
  var empty = [];
  firstIndex = arr[0];
  secondIndex = arr[1];

  if (firstIndex > secondIndex){
    for (var secondIndex=0 ; secondIndex < arr.length; i++){
    empty.push()
  }    
  }

  
  return 1;
}

sumAll([1, 4]);
